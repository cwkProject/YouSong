<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.yousong.yousong"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:name=".common.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.AdDetailActivity"/>
        <activity android:name=".activity.ShareFriendActivity"/>
        <activity
            android:name=".activity.WithdrawActivity"
            android:windowSoftInputMode="stateAlwaysVisible"/>
        <activity android:name=".activity.MyAdActivity"/>
        <activity android:name=".activity.PersonalAuthenticationActivity"/>
        <activity android:name=".activity.EnterpriseCertificationActivity"/>
        <activity android:name=".activity.PublishAdActivity"/>
        <activity android:name=".activity.RechargeActivity"/>
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:exported="true"/>
    </application>

</manifest>