<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.yousong.yousong"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:name=".global.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name=".activity.common.SplashActivity"
            android:theme="@style/AppTheme.SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.common.MainActivity"
            android:launchMode="singleTask">
        </activity>
        <activity android:name=".activity.ads.AdDetailActivity"/>
        <activity android:name=".activity.ads.ShareFriendActivity"/>
        <activity
            android:name=".activity.user.WithdrawActivity"
            android:windowSoftInputMode="stateAlwaysVisible"/>
        <activity android:name=".activity.ads.MyAdActivity"/>
        <activity android:name=".activity.user.PersonalAuthenticationActivity"/>
        <activity android:name=".activity.user.EnterpriseCertificationActivity"/>
        <activity android:name=".activity.ads.PublishAdActivity"/>
        <activity android:name=".activity.user.RechargeActivity"/>
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:exported="true"/>
        <activity android:name=".activity.user.LoginActivity"/>
    </application>

</manifest>